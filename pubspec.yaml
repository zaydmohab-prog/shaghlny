name: shaghlny
description: SHAGHLNY Service Marketplace
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase - updated for iOS compatibility
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  
  # Auth
  google_sign_in: ^6.2.0
  
  # Maps
  google_maps_flutter: ^2.7.0
  
  # Location
  geolocator: ^13.0.0
  
  # Utils
  intl: ^0.19.0
  flutter_rating_bar: ^4.0.1
  image_picker: ^1.1.0
  uuid: ^4.4.0
  provider: ^6.1.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png